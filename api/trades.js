const {get,N}=require('./_lib');module.exports=async(req,res)=>{res.setHeader('Cache-Control','s-maxage=15, stale-while-revalidate=30');const pools=String(req.query?.pools||req.query?.pool||'').split(',').filter(Boolean).slice(0,5);let trades=[];for(const pool of pools){try{const d=await get('https://api.geckoterminal.com/api/v2/networks/solana/pools/'+encodeURIComponent(pool)+'/trades?trade_volume_in_usd_greater_than=1000',30000);for(const x of d.data||[]){const a=x.attributes||{},usd=N(a.volume_in_usd);if(usd>=1000)trades.push({pool,side:String(a.kind||a.side||'unknown').toUpperCase(),usd,timestamp:a.block_timestamp||a.timestamp||null,tx:String(a.tx_hash||''),maker:String(a.tx_from_address||a.maker||''),url:'https://geckoterminal.com/solana/pools/'+pool})}}catch{}}trades.sort((a,b)=>b.usd-a.usd);res.status(200).json({data:trades.slice(0,40),source:'GECKOTERMINAL LIVE'})};